<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title><%= title %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap');

        body {
            background: linear-gradient(to right, #1e3c72, #2a5298);
            font-family: 'Open Sans', sans-serif;
            color: white;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        header {
            padding-right: 3%;
            margin-bottom: 20px;
        }

        h1 {
            background-color: #006bdd;
            padding: 10px 20px;
            border-radius: 5px;
            display: inline-block;
            text-shadow: 2px 2px 4px #000000;
        }

        h1 span {
            color: #ffcc00;
        }

        .tagline {
            font-size: 1.2em;
            color: #ffcc00;
            margin-bottom: 10px;
            text-shadow: 1px 1px 2px #000000;
        }

        h2 {
            color: #9affd8;
            text-shadow: 1px 1px 5px rgb(0, 157, 255);
        }

        .container {
            background-color: rgba(0, 0, 0, 0.75);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 20px #00ff99;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"],
        select {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #ddd;
            box-sizing: border-box;
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        input[type="text"]:focus,
        input[type="email"]:focus,
        input[type="password"]:focus,
        select:focus {
            outline: none;
            border: 1px solid #00ff99;
            box-shadow: 0 0 10px #00ff99;
        }

        button {
            background-color: #9e0500;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            box-sizing: border-box;
        }

        button:hover {
            background-color: #cc0404;
        }

        a {
            color: #fff242;
        }

        a:hover {
            text-decoration: underline;
        }

        .error {
            background-color: #ffcccc;
            color: #990000;
            padding: 10px;
            border-radius: 5px;
            margin: 10px 0;
        }
    </style>
</head>

<body>
    <header>
        <h1><span>S</span>PORTS SHEDULER</h1>
        <div class="tagline">Elevate your game with ease!</div>
        <small>new to sports?</small>
        <p>Signup for a new account</p>
    </header>

    <div class="container">
        <h2>Signup as a new user</h2>
        <form action="/users" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input required type="text" name="firstName" placeholder="First Name"><br>
            <input required type="text" name="lastName" placeholder="Last Name"><br>
            <input required type="email" name="email" placeholder="Email"><br>
            <select required name="role">
                <option value="" disabled selected hidden>Choose a role</option>
                <option value="player">Player</option>
                <option value="admin">Admin</option>
            </select><br>
            <input required type="password" name="password" placeholder="Password"><br>
            <button type="submit">Sign Up</button>
        </form>
        <% if (messages && messages.error && messages.error.length > 0) { %>
            <% for (var i = 0; i < messages.error.length; i++) { %>
                <div class="error"><%= messages.error[i] %></div>
            <% } %>
        <% } %>
    </div>
</body>

</html>
